# Reference files have moved

Evolving reference files now live in the **project repo** at `~/projects/future/references/`:
- `skill-assessment.md` — skill level rubric and strategic value criteria

This avoids plugin cache drift — the SKILL.md reads these from the project root (CWD), not from the plugin cache.
