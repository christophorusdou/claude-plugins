---
description: Assemble a coordinated agent team for your task
argument-hint: [feature|review|research|debug|maintain|architect]
---

# Agent Teams

Assemble and coordinate a specialized agent team.

## Arguments

The user invoked this command with: $ARGUMENTS

## Instructions

1. Invoke the `agent-teams` skill
2. If $ARGUMENTS specifies a team type (`feature`, `review`, `research`, `debug`, `maintain`, `architect`), use it directly
3. If no argument provided, use the auto-detection logic from the skill to determine the best team
4. Follow the skill's phase sequence and checkpoint format exactly
